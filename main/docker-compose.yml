version: "2.1"
services:
    register:
        build:
            context: .
            dockerfile: Dockerfile
        command: python -u main_flask.py
        env_file: .env
        restart: always
